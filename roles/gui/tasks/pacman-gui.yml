---
- name: Manually update apt-key until bus is fixed
  shell: apt update
  become: yes

- name: Apt Installs GUI tools
  become: yes
  pacman:
    state: present
    name:
      - clipit
      - gkrellm
      - gnome-do
      # - google-chrome-stable
      - kdiff3
      - kleopatra
      - rhythmbox
